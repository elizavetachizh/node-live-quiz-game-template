{
  "name": "live-quiz-game",
  "version": "1.0.0",
  "description": "Real-time Live Quiz Game with WebSocket",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "start": "npm run build && concurrently -k -n server,client \"npm run start --workspace=server\" \"npm run start --workspace=client\"",
    "start:server": "npm run build --workspace=server && npm run start --workspace=server",
    "start:client": "npm run build --workspace=client && npm run start --workspace=client",
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "install:all": "npm install && npm install --workspace=server && npm install --workspace=client"
  },
  "keywords": [
    "websocket",
    "quiz",
    "game",
    "real-time"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "crypto": "^1.0.1",
    "prettier": "^3.8.1"
  }
}
